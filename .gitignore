# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.g.dart
*.freezed.dart

# IDE
.idea/
.vscode/settings.json
*.iml

# Environment
.env
.env.local
*.pfx

# Signing — never commit a release keystore or its passwords. Codemagic holds these
# (Code signing identities > Android keystores; the yaml's android_signing reference).
*.jks
*.keystore
key.properties

# OS
.DS_Store
Thumbs.db

# Impeccable decision-page scratch state
.impeccable/

# Superpowers scratch state (brainstorm mockups, SDD workspaces)
.superpowers/

# Superpowers isolated worktrees
.worktrees/
