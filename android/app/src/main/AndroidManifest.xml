<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:label="tasktap_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--
            AppAuth's two activities, pulled into MainActivity's task.

            MainActivity declares android:taskAffinity="" — Flutter's template sets it so another
            app cannot claim this one's task. AppAuth's activities arrive from the library manifest
            with the default affinity (the application id), which puts them in a DIFFERENT task.

            The sign-in then fails at the last step: the browser redirects to
            RedirectUriReceiverActivity, which starts AuthorizationManagementActivity in that other
            task, where nothing holds the pending request. AppAuth logs

                W/AppAuth: No stored state - unable to handle response

            and the app sits on the login screen having done everything right. Both activities are
            given the same empty affinity here so the redirect comes back to the task that is
            waiting for it. tools:replace, because the library already sets the attribute.
        -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:taskAffinity=""
            android:exported="true"
            tools:replace="android:taskAffinity" />
        <activity
            android:name="net.openid.appauth.AuthorizationManagementActivity"
            android:taskAffinity=""
            tools:replace="android:taskAffinity" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
