/// # TaskTap mobile — direction contract
///
/// Superseded 2026-09-03. Both the identity this doc comment used to describe
/// (Cassetta: gunmetal case shell, safety orange, Sora/Manrope, a latch-row
/// bottom nav) and the one before it (Allestimento Furgone / van-rack, brand
/// yellow) are retired. The app is now Il Documento — an Italian back-office
/// paper-and-stamp system, aligned to the web app's own `DESIGN.md`
/// (`../../../frontend/DESIGN.md` from this repo's root) and, since
/// 2026-09-16, this project's own `DESIGN.md` at its root too, stating
/// mobile's own translation of the world. Stamp red (`AppColors.Y`, `#C03221`), carbon
/// ink, paper/sheet surfaces, Archivo Narrow/Archivo/IBM Plex Mono, flat
/// "no glass, no floating cards" material, a flat-sheet bottom nav. See
/// PRODUCT.md's Brand Commitments section for the full supersession record,
/// including a 2026-09-16 critique's findings on how much of this world had
/// actually reached the shipped screens.
library;

export 'app_colors.dart';
export 'app_palette.dart';
export 'app_rack.dart';
export 'app_spacing.dart';
export 'app_text_styles.dart';
export 'app_theme.dart';
export 'status_colors.dart';
