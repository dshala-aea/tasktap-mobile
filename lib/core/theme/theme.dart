/// # TaskTap mobile — direction contract
///
/// Superseded 2026-08-23. The identity below (Allestimento Furgone / van-rack
/// world, brand yellow, seed 1e21eb0b) was replaced after the user reopened
/// every prior brand commitment — color, typefaces, nav pattern — and ran a
/// fresh Impeccable new-work round. Three full HTML prototypes were built on
/// identical content and compared side by side; the user chose the
/// assigned-by-roll direction. See PRODUCT.md's Brand Commitments section for
/// the full record.
///
/// THESIS: the app is your case — every module a labeled, tool-shaped
/// compartment. An empty dashed silhouette means "still needed"; a filled
/// slot means "accounted for." It refuses the field-service default: white
/// cards on grey with a KPI row.
///
/// OWN-WORLD: gunmetal case shell, foam-gray compartment lining, safety
/// orange (`AppColors.Y`, `#FF7A2E`) reserved strictly for what needs
/// attention, is selected, or is still unfinished — never a live/running
/// state, which pulses green instead (see `LiveDot`). Depth is the rack's
/// existing pitch and ledge (still the right primitive — the old world's
/// structure already spoke this grammar), never a floating card shadow.
/// Sora on the label, Manrope in the cell. The bottom nav is the case's own
/// latch row, not a floating pill.
///
/// STORY: the technician reads state the way they'd read their own tool case
/// — what's strapped in as needing attention, what's queued, what's missing
/// — and records the job without a second look.
///
/// FIRST VIEWPORT: Dashboard. A dark case-shell ID plate up top (who, when,
/// how much is left); a fixed rail down the leading edge; ticket cells below
/// on constant pitch; the queue as silhouetted empty cells; a running clock
/// marked by a pulsing green dot, never the accent.
///
/// FORM: Cassetta, candidate 4 of 7 (self-authored list: cassetta attrezzi,
/// libretto di manutenzione, quadro elettrico, cartellino orario, bolla a
/// ricalco, cantiere signage, targhetta), seed key cce7d144.
///
/// FINISH: unreviewed and undocumented is unfinished; this build ends with the
/// finish review, the verdict, and DESIGN.md
library;

export 'app_colors.dart';
export 'app_palette.dart';
export 'app_rack.dart';
export 'app_spacing.dart';
export 'app_text_styles.dart';
export 'app_theme.dart';
export 'status_colors.dart';
